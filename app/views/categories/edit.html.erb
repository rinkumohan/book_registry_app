<div class="breadcrumbs breadcrumb_style">
  <span class="fa fa-home"></span> / Category / <span class="active">Edit</span>
</div>
<div>
  <%= form_with model: @category do |form|%>
    <%= render "form", form: form%>
  <%end%>
</div>
